/**
 * =============================================================================
 *  怪物难度调节器 · 外部配置文件（在游戏外面用记事本改这里就行）
 * -----------------------------------------------------------------------------
 *  改完保存 → 重新进入世界即可生效（首次加载、或执行 /scriptevent dt:reset 后生效）。
 *  游戏内用界面改的值会临时覆盖这里；想让本文件重新生效，执行 /scriptevent dt:reset
 *  或在世界里删掉本包的存档设置即可。
 *
 *  取值范围：
 *    speed  移动速度倍率  0.1 – 4.0   （1.00 = 原版速度）
 *    damage 伤害倍率     0   – 6.0   （1.00 = 原版伤害，0 = 完全无伤）
 *    health 生命值倍率    0.1 – 8.0   （1.00 = 原版血量）
 *    spawn  刷新数量倍率  0   – 6.0   （1.00 = 原版刷怪量，0 = 不刷怪）
 * =============================================================================
 */

export const CONFIG = {
  enabled: true,        // 总开关：true 启用，false 全部失效
  speed: 1,             // 移动速度倍率
  damage: 1,            // 伤害倍率
  health: 1,            // 生命值倍率
  spawn: 1,             // 刷新数量倍率
  affectBoss: false,    // 是否同时影响 Boss（末影龙 / 凋灵 / 监守者 / 远古守卫者）
  affectNeutral: false  // 是否同时影响中立生物（狼 / 铁傀儡 / 猪灵 / 末影人 等）
};
